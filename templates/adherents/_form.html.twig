{{ form_start(form) }}
  {# {{ form_widget(form) }} #}
  {{ form_row(form.first_name) }}
  {{ form_row(form.last_name) }}
  {{ form_row(form.tel) }}
  {{ form_row(form.email) }}


    {% for adhesion in form.adhesions %}
      {{ form_row(adhesion.starting_date) }}
      {{ form_row(adhesion.ending_date) }}
    {% endfor %}  


  <div class="d-grip-gap-2">
    <input type="submit" value="{{ submitButtonText|default('Ajouter un adhérent') }}" class="btn btn-primary" formnovalidate="formnovalidate">
  </div>

{{ form_end(form) }}